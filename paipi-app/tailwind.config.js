/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{html,ts}", // This tells Tailwind to look at all HTML and TypeScript files in the src folder
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
